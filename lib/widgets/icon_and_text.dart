import 'package:flutter/material.dart';

class IconAndTextWidget extends StatelessWidget {
  const IconAndTextWidget({super.key, required IconData icon});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}